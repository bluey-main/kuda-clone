import 'package:flutter/material.dart';

class Borrow extends StatelessWidget {
  const Borrow({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Borrow"),),
    );
  }
}